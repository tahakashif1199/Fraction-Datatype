# Fraction-Datatype
A custom Python implementation of a Fraction datatype using Object Oriented Programming (OOP). Features magic methods for arithmetic operations and robust exception handling for mathematical constraints (e.g., zero denominator).
